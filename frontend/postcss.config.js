export default {
  plugins: {
    // Note: Tailwind v4 CSS processing is handled by @tailwindcss/vite (see vite.config.js).
    // Do NOT add tailwindcss here — it causes double-processing conflicts with the Vite plugin.
    autoprefixer: {},
  },
}
